# 数据目录

本目录只保存可重建的半合成快照和被 Git 忽略的本地事件数据库。生成数据包含岗位发布
时间，以及带位置、点击、停留、收藏和投递的离线 Feed 曝光。

| 产物 | 用途 | 生命周期 |
|---|---|---|
| `jobrec_events.sqlite3` | 唯一曝光与行为反馈 | 本地演示状态，可删除重建，不提交 |

服务数据由 `src/data/generator.py` 固定种子生成，并由 bundle 的 `data_sha256` 校验。
禁止放入真实简历、生产导出、密钥或未经治理的个人数据。
