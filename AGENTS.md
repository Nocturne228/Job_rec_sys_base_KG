# JobRec-Feed 面试项目开发规约

本规约适用于仓库中的代码、测试、文档和产物。项目的首要目标是形成一段诚实、完整、
容易讲清楚的求职项目经历，展示推荐系统问题拆解、关键技术选择、工程边界和难题解决
思路；它不是生产招聘平台，也不以补齐所有企业级基础设施为目标。

## 1. 项目定位与事实底线

项目围绕一条最小但完整的故事主线展开：

```text
半合成曝光 → 时间切分 → 多路 Top-K 召回 → 学习排序 → 流量重排
            → 已知/冷启动服务 → 证据受限兴趣扩展 → 曝光行为反馈
```

必须接受以下事实：

1. 原始真实数据已经丢失，当前数据为固定种子生成的半合成数据；
2. 项目用于证明技术思路、代码路径和接口能够工作，不能证明真实用户效果或业务收益；
3. 可以介绍生产化设计思路、预期指标和容量推导，但不能把它们写成已经落地的结果；
4. 旧材料中的 `23k 节点`、`168k 边`、`80% 有效率`、`1000 并发`等只能标为赛题
   背景、历史未验证说法或未来目标，不能作为当前成果。

禁止虚构真实数据量、用户量、公司背景、个人职责、上线状态、A/B 结果或业务提升。

## 2. 证据类型与未实测指标

文档使用以下证据标签：

- **已验证**：当前仓库在写明的环境和命令下实际复现；
- **已实现**：代码路径存在并有最小可运行证据，但未验证目标环境或规模；
- **推导/预期**：尚未实测，由复杂度、样本计算、公开资料或工程经验推导；
- **目标**：未来希望达到的验收值，不是当前结果；
- **环境受限**：实现需要外部数据、服务或运行时，本次无法验证；
- **历史未验证**：来自旧材料且原始证据已经丢失。

文档允许出现尚未测试的指标，但每个此类指标必须同时写明：

1. 指标名称、公式、统计口径和单位；
2. 依据来源，例如算法复杂度、单请求操作量、依赖基准、公开论文或相似系统经验；
3. 输入规模、硬件、并发、缓存、模型和数据分布等关键假设；
4. 合理范围或数量级，不使用无依据的精确小数；
5. 失效条件、不能证明的事项和未来验证方案；
6. 明确标签为“推导/预期”或“目标”，不能与实测表格混排后省略状态。

示例：可以写“**推导/预期**：在 100 个候选、384 维已缓存向量下，精排主要为
`O(CF)` 的向量与线性运算，单请求应处于毫秒到几十毫秒量级；该判断不含网络模型编码，
需用固定硬件压测验证。”不能写“P95 为 20 ms”而没有实测。

发生冲突时，事实优先级为：源代码与测试 → 版本化模型/结果 → 数据与评估文档 →
架构文档 → README/面试指南 → 外部资料与历史文件。

## 3. 面试故事优先原则

功能和工程投入按以下顺序取舍：

1. 能否解释一个真实的推荐系统问题；
2. 能否展示有依据的技术选择和替代方案；
3. 能否形成“问题—分析—方案—取舍—证据—限制”的难题解决案例；
4. 是否保护数据泄漏、离线/在线不一致和解释不一致等关键可信度；
5. 最后才考虑生产级扩展、框架丰富度和测试覆盖率。

允许用接口、协议和文档表达尚未落地的生产思路。不要为了显得“生产化”而增加没有
主线用途的数据库、消息队列、微服务、模型、指标模块或大量故障分支。一个模块若不能
增强主故事、提供可运行证据或形成高质量面试追问，应删除或只保留在未来计划中。

## 4. 当前功能范围

### 4.1 必须保留的可运行主线

1. 固定种子半合成用户、岗位、技能、曝光、先修关系和行为；
2. 逐用户时间留出，训练图、热度和近期兴趣只使用训练交互；
3. LightGCN、文本、技能和热度/新鲜度四路 Top-K 召回及来源证据；
4. 简单基线、固定融合、Pointwise 和多阶段模型在共享协议下比较；
5. 已知用户屏蔽训练期岗位，用户/岗位冷启动不伪造协同历史；
6. 六特征学习排序与确定性流量重排，贡献之和能够核对最终分数；
7. 带方向、置信度和来源的 `PREREQUISITE_OF` 学习路径；
8. 证据受限的可选 LLM 兴趣/query 扩展和确定性降级；
9. 覆盖完整在线行为的 `ModelBundle v3` 和 FastAPI 只读加载；
10. 曝光与点击、停留、收藏、投递或满意度反馈的精确关联。

### 4.2 只作为计划或讨论的能力

以下能力默认不实现，只在有明确求职岗位需求时增加：

- 真实招聘数据接入、ESCO/O*NET 映射和 point-in-time 特征回放；
- 预训练双塔、向量数据库或 ANN 服务；
- GAT、DIN/Transformer、多任务 LTR 和 LLM 用户模拟；
- Neo4j、消息队列、在线特征库、模型注册与灰度发布；
- 招聘方反向匹配、趋势分析、公平性认证和正式 A/B 平台；
- OIDC、KMS、完整隐私治理与生产安全审计；
- 多 worker、耐久、故障注入和生产容量测试。

讨论这些能力时应说明为什么当前不做、触发条件、候选方案和验证计划。

## 5. 模块体系

```text
src/data/          领域模型、半合成曝光、时间切分和类型化技能图
src/recall/        LightGCN、离线文本和多路候选协议
src/ranking/       共享特征、基线、Pointwise、重排和可加解释
src/generation/    证据受限兴趣扩展和确定性降级
src/models/        ModelBundle 发布契约
src/experiments/   共享协议下的基线与消融
src/metrics/       唯一曝光/行为反馈事件存储
src/api/           已知/冷启动推荐、能力评估与反馈 API
src/utils/         训练和可复现性工具
scripts/           bundle、实验和轻量演示入口
tests/             少量关键不变量和 API 故事测试
docs/              架构、证据、面试表达和问题解决记录
```

公共入口只做参数解析和编排。算法、切分、排序、解释和持久化逻辑分别放在所属模块，
不得在根入口或 API 路由中复制第二套实现。

## 6. 不可破坏的技术约束

只有以下约束需要强制测试保护：

1. 协同训练图、流行度、近期兴趣和训练统计只使用训练交互；
2. BPR 负样本不能来自用户的训练期已见岗位；
3. 离线评估和已知用户在线推荐必须先屏蔽 seen，再生成候选/特征；
4. 已知与冷启动路径必须显式不同；
5. 候选必须保留召回来源；解释、排序与重排使用相同特征和版本化参数；
6. 学习路径只使用有方向、置信度和来源的 `PREREQUISITE_OF` 边；
7. 服务只加载已发布 bundle，请求内不能训练或改写模型；
8. Bundle 的 Serving 哈希必须覆盖数据、协同权重、排序、文本和重排配置；
9. 生成兴趣/query 必须有输入证据，非法或不可用输出必须确定性降级；
10. 反馈必须对应已记录的唯一曝光，同一曝光最多反馈一次。

其他生产级约束应以设计说明和验证计划表达，不要求为了“完整”写大量模拟实现。

## 7. 代码、依赖与产物规范

- Python 版本以 `pyproject.toml` 为准；公共边界使用类型注解和 Pydantic 模型。
- 随机过程显式传入种子；默认路径离线运行，不自动下载模型或访问互联网。
- 统一使用 `uv`，不增加 `requirements.txt`、Conda 文件或临时安装脚本。
- 核心依赖只保留默认主线实际使用的库；未来能力优先写计划，需要运行时再用 extras。
- 路由只做校验与编排；边界异常必须可诊断。
- 注释解释约束和取舍，不用“生产级”“行业领先”等词装饰实现。
- `models/jobrec_bundle.json` 与 checkpoint 成对生成；使用不可变版本或内容哈希，不能
  先覆盖旧 checkpoint 再写新 bundle。
- `results/` 只保存可追溯的机器可读实测；推导/预期指标写入文档，不伪造结果 JSON。
- 禁止提交真实简历、个人信息、密钥、`.env`、本地 SQLite、日志或不可信 pickle。

## 8. 实验与测试策略

实验的目的不是证明复杂模型领先，而是展示正确协议、基线意识和结果解释。模型比较需
共享数据版本、候选池、切分、seen masking、种子和指标定义；负面结果应保留。

测试只覆盖会破坏主故事的高风险反例，不追求覆盖率数字：

- 切分、训练图、负采样和 seen masking；
- 多路候选来源、已知/冷启动模式与可加解释；
- 生成兴趣证据约束和确定性降级；
- bundle 一致性和只读加载；
- 类型化学习路径；
- 曝光—反馈精确关联；
- 一条端到端 API 主故事。

默认质量门禁：

```bash
uv run python -m compileall -q src scripts tests main.py
uv run python -m pytest -q
uv run black --check src scripts tests main.py
uv run isort --check-only src scripts tests main.py
uv run mypy src/api/routes.py src/models/bundle.py src/metrics/event_store.py \
  src/ranking src/recall src/generation/profile_expansion.py src/data/loader.py
```

不设置覆盖率下限。外部服务、容器和性能没有环境时，只需静态检查、合理推导并写明验证
方法；不需要用大量 mock 或脆弱测试制造“全部通过”的表象。

## 9. 文档体系

- 根 `README.md`：项目入口、故事主线、边界、快速演示；
- `docs/architecture.md`：技术要求、模块边界、数据流和功能计划；
- `docs/data-and-evaluation.md`：数据、指标定义、实测、推导/预期和限制；
- `docs/interview-guide.md`：简历表述、演示顺序、技术追问与取舍；
- `docs/problem-solving.md`：少量最有价值的难题案例；
- `docs/README.md`：阅读路线和事实归属；
- 目录 README：产物用途与生命周期。

文档先讲当前事实，再讲技术判断，最后讲未来计划。一项易变事实只设一个详细来源。
问题记录使用“问题/影响—分析—解决—证据—剩余边界”，不复制 Git 历史或完整架构。

## 10. Git 与完成定义

- 保留用户已有的无关改动；修改前后检查 `git status` 和 `git diff`。
- 新文件放入职责目录，不在根目录新增临时报告、截图、日志或第二套依赖清单。
- 删除模块时同步删除导入、测试、依赖、文档和产物引用，不建立 `archive/`。
- 任务完成时应满足：主故事可运行、核心约束未破坏、最小门禁通过、文档与代码一致、
  没有断链或夸大结论，并清楚说明哪些是实测、推导/预期和未来目标。
