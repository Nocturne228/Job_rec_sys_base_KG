# JobRec-KG 开发与文档规约

本文件是仓库级最高优先级的协作规约，适用于项目根目录下的全部代码、测试、
脚本、文档和产物。人类贡献者与自动化代理都必须遵守。

## 1. 项目定位与两条硬约束

JobRec-KG 的当前目标是形成一段**可运行、可解释、可复核的求职项目经历**，展示
推荐系统、知识图谱、模型发布、API 和工程质量能力，而不是经营真实招聘业务。

开发和写作必须同时接受以下事实：

1. 原始真实数据已经丢失，仓库中不存在可恢复、可校验的真实训练集和线上日志。
2. 当前数据均为固定种子生成的半合成数据，只能验证代码路径、接口契约、评估
   协议与工程集成，不能证明真实用户效果、线上收益、生产规模、公平性或合规性。

禁止为了让经历“更像生产项目”而虚构或补写历史数据规模、用户量、业务提升、
公司背景、个人职责、上线状态或真实 A/B 结果。旧材料中的 `23k 节点`、
`168k 边`、`80% 有效率`、`1000 并发`等数字，除非明确标为“赛题目标”或
“历史说法且无法验证”，不得作为本项目成果复述。

## 2. 事实来源与证据标签

发生冲突时，按以下顺序判断：

1. 源代码、测试、`pyproject.toml`、`uv.lock`；
2. `models/`、`results/` 中可追溯的版本化产物；
3. [`docs/data-and-evaluation.md`](docs/data-and-evaluation.md) 中的协议与实测记录；
4. [`docs/architecture.md`](docs/architecture.md) 中的设计契约；
5. 根 `README.md` 和 [`docs/interview-guide.md`](docs/interview-guide.md) 的摘要；
6. `ref/` 中的外部资料和历史赛题文件。

文档中的能力或结论使用以下标签：

- **已验证**：当前仓库在写明的环境和命令下实际复现；
- **已实现**：代码路径存在，但未在目标环境或规模下验证；
- **部分实现**：存在可用子集，并明确缺口；
- **目标**：未来希望达到，不是当前结果；
- **环境受限**：实现已提供，但本次缺少外部服务或运行时；
- **历史未验证**：来自旧材料，因原始证据丢失而无法确认。

不得用“支持”“完成”“达到”“生产可用”等模糊词绕过标签。外部论文和业界
案例只能解释选型，不能证明本仓库达到同样指标。

## 3. 仓库职责

```text
src/              领域代码、模型、推荐、图谱、生成、API 与指标
scripts/          模型发布、实验、图导入和负载冒烟入口
tests/            构造式回归测试与服务契约测试
data/             可重建半合成数据和被忽略的本地运行状态
models/           离线发布的模型 bundle 与 checkpoint
results/          协议明确、机器可读的验证结果
docs/             维护中的设计、证据和面试说明
ref/              外部研究资料与历史赛题文件，不是本地实现证据
logs/             被忽略的本地日志占位目录
```

新文件必须放入拥有该职责的目录。不要在根目录新增临时报告、计划、截图、日志、
数据副本或第二套依赖清单。

## 4. 不可破坏的架构约束

除非任务明确修改设计并同时增加回归测试，否则必须保留：

1. 协同训练图、流行度和训练统计只使用训练交互；
2. BPR 负样本只能来自该用户未观察过的岗位；
3. 离线评估和在线已知用户推荐都屏蔽训练期已见岗位；
4. 离线状态通过校验后的 `ModelBundle` 发布，服务端不得静默重训或重建不一致映射；
5. 已知用户与冷启动用户走显式不同路径，并在响应证据中可观察；
6. 排序归一化限定在单次请求候选集，不跨请求共享拟合状态；
7. 解释必须使用与最终分数完全相同的归一化特征和权重，贡献之和可核对；
8. 学习路径只使用带方向、置信度和来源的 `PREREQUISITE_OF` 边；
9. 外部 LLM 是可选适配器，输出必须经过 Pydantic 校验，失败时使用确定性证据兜底；
10. 反馈必须关联已记录曝光，不能把无曝光上下文的反馈解释为推荐效果；
11. 模型在 FastAPI lifespan 中加载，请求处理器不得执行训练侧状态变更。

修改模块边界、离线/在线流程或上述约束前，先阅读
[`docs/architecture.md`](docs/architecture.md)。

## 5. 代码开发规范

### 5.1 Python、类型与结构

- 支持 `pyproject.toml` 声明的 Python 版本，不在文档中另写冲突版本。
- 模块、函数、变量使用 `snake_case`；类和 Pydantic 模型使用 `PascalCase`；
  常量使用 `UPPER_SNAKE_CASE`。
- 公共函数、服务边界、适配器和持久化接口必须有参数与返回类型。
- API 请求/响应使用显式 Pydantic 模型；改变已有响应结构时必须同步契约测试，
  必要时升级版本，不得静默返回另一种形状。
- 路由只做校验、鉴权和编排；可测试的领域逻辑放入所属模块。
- 边界错误要可诊断。除文档规定的 LLM 兜底外，不得把异常静默包装成成功。
- 优先删除死代码和重复实现；兼容分支必须有调用方、测试或清晰的退役条件。

### 5.2 可复现性与外部依赖

- 统一使用 `uv`：`uv sync --frozen --extra dev`、`uv run <command>`。
- 运行依赖写入 `[project].dependencies`，开发依赖写入 `dev`；声明变化必须更新
  `uv.lock`。禁止增加 `requirements.txt`、Conda 文件或临时安装脚本。
- 默认测试路径必须离线可运行，不自动下载预训练权重，不依赖 Neo4j、外部 LLM、
  浏览器或互联网。
- 随机过程显式传入种子；测试隔离文件、数据库、环境变量和时钟。
- 密钥、口令、连接串通过环境变量传入，绝不提交 `.env` 或真实凭据。

### 5.3 格式、注释与日志

- Black 行宽和 Isort 配置以 `pyproject.toml` 为准，不另设局部风格。
- 注释解释“为什么”和约束，不复述语句。不得保留与实现不符的生产化叙述。
- 面向开发者的自然语言以中文为主；公开标识、协议字段和必要术语保留英文。
- 禁止记录原始简历、个人字段、Bearer token、密码、加密密钥或 LLM API key。

## 6. 数据、实验与模型产物规范

- `data/mock_data.pkl` 是可信本地的可重建半合成产物，不是版本化真实数据集；
  禁止反序列化不可信 pickle。
- 生成器只能构造可学习但透明的演示分布，不得把生成时使用的 oracle 直接泄漏到
  模型特征或测试标签中。
- 修改切分、采样、屏蔽、候选生成或指标定义时，必须增加一个在发生泄漏时会失败
  的构造式回归测试。
- 模型比较必须共享数据版本、候选池、切分、种子策略和指标定义；保留负面或中性
  结果，不因复杂模型输给简单基线而删除基线。
- 数值结论至少记录：数据/生成器版本、规模、切分、种子、模型配置、运行环境、
  命令、时间和机器可读产物。
- `models/jobrec_bundle.json` 是在线发布契约。字段变化需要 schema 版本或向后兼容
  默认值；ID 映射必须连续且与 checkpoint 张量一致。
- 模型、数据和结果产物由脚本生成，不得手工修改数字或权重来让测试通过。
- 受保护属性只有在目的合法、治理明确并获得适当数据时才能使用；合成分组诊断
  不是公平性认证。

## 7. 测试与质量门禁

变更按风险运行最小充分检查；完整本地门禁为：

```bash
uv run python -m compileall -q src scripts tests main.py
uv run python -m pytest -q --cov=src --cov-report=term-missing --cov-fail-under=50
uv run black --check src scripts tests main.py
uv run isort --check-only src scripts tests main.py
uv run mypy src/api/routes.py src/models/bundle.py src/data/graph_store.py \
  src/generation/adapters.py src/security.py src/data/models.py \
  src/metrics/event_store.py src/data/private_profile_store.py \
  src/utils/crypto.py
```

附加要求：

- 数据/模型契约：重建 bundle，并运行实验套件；
- API：运行 API 契约测试；延迟相关变更再运行本地负载冒烟；
- Neo4j：有服务时做集成验证，无服务时只能标记“环境受限”；
- 文档：检查相对链接、文件名、命令、环境变量、过时引用和不受支持的结论。

工具或外部服务不可用时，记录限制并完成所有安全的本地替代检查，不得声称通过。

## 8. 文档体系与写作规范

[`docs/README.md`](docs/README.md) 是文档索引和事实归属表。维护文档只包括：

- 根 `README.md`：项目入口、边界、能力摘要、快速开始；
- `docs/architecture.md`：组件边界、数据流、设计决策、运行时与故障模式；
- `docs/data-and-evaluation.md`：数据契约、生成/切分协议、指标、实测结果和限制；
- `docs/interview-guide.md`：基于上述事实的简历表述、演示顺序和问答；
- `docs/problem-solving.md`：开发中已关闭问题的症状、分析思路、解决办法、回归证据
  和遗留限制；
- `ref/README.md`：外部资料使用规则与精简索引；
- `data/`、`models/`、`results/`、`logs/` 下的 README：目录级产物生命周期。

写作时遵守：

1. 一项易变事实只设一个详细来源，其他文档只给摘要和链接；
2. 先写当前事实，再写限制，最后才写未来方向；
3. 命令必须能从仓库根目录复制执行；路径使用项目相对链接；
4. 数字紧邻协议、日期和证据标签，不写无来源的“提升”“高并发”“低延迟”；
5. 图表必须解释关系且与代码一致，装饰性架构图和重复流程图应删除；
6. 外部资料使用转述并可追溯到原文件，不大段复制，不把论文结果移植成本项目结果；
7. Markdown 文件使用职责明确的英文 `kebab-case.md`，标准元数据保留 `README.md`
   和 `AGENTS.md`；不使用编号前缀、版本后缀或“最终版”等名字；
8. 每次解决具有可复用价值的问题时，更新 `docs/problem-solving.md`。每条记录必须
   包含问题/影响、定位与分析思路、采取的解决办法、验证命令或测试、仍未覆盖的边界；
   不记录密钥、个人数据或无关的逐步操作日志。
9. `problem-solving.md` 解释“为何这样改”，不能复制架构说明、完整实验表或 Git
   提交历史；不维护第二份 Changelog、整改计划、无边界复盘报告或正式论文。

行为变化必须在同一次修改中更新代码、测试和事实归属文档。过时文档在唯一信息迁移
后直接删除，不放入 `archive/` 制造第二套事实来源。

## 9. 安全、隐私与演示边界

- 当前 HMAC Bearer 与默认账号仅为自包含演示控制，共享部署必须覆盖默认密钥和口令。
- 个人字段使用 AES-GCM 认证加密，不得替换为编码、确定性加密或无认证模式。
- 安全代码变化必须测试篡改拒绝、过期和权限边界。
- “已实现加密/鉴权演示”不等于“安全审计通过”“隐私合规”或“生产安全”。
- 演示中只使用生成数据，不导入真实简历或个人信息。

## 10. Git 与工作区卫生

- 保留用户已有的无关改动；修改前后检查 `git status` 和 `git diff`。
- 不提交虚拟环境、缓存、覆盖率数据库、日志、本地 SQLite、`.env` 或系统文件。
- 不修改生成缓存和本地数据库；只有任务要求重建时才更新模型或结果产物。
- 避免破坏性历史操作。删除文件前确认其信息已迁移、确实重复/过时或在任务范围内，
  并同步清理所有引用。

## 11. 完成定义

任务完成必须同时满足：

1. 请求的代码或文档结果已经落地；
2. 两条项目硬约束和架构不变量未被破坏；
3. 相关测试、格式、类型和链接检查通过，或明确记录环境限制；
4. 依赖声明与锁文件一致；
5. 公开命令、接口、数据语义、图示和证据文档同步；
6. 没有遗留旧文件名、断链、秘密、生成中间物、重复事实源或夸大结论；
7. 交付说明清楚列出改动、验证和仍不能由当前仓库证明的事项。
